<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC 
    "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.easy.dao.AllotRoleDao">

	<!-- 增加操作 -->
	<insert id="create" parameterType="AllotRole">
		INSERT INTO
			admin(
				userId,roleId,createTime,createUser,modifyTime,modifyUser
			)
		VALUES(
			#{userId},#{roleId},#{createTime},#{createUser},#{modifyTime},#{modifyUser}
		)
	</insert>

	<!-- 更新操作 -->
	<update id="update" parameterType="Admin">
		UPDATE
			ADMIN
		<set>
			<if test="createTime!=null">
			createTime = #{createTime},
			</if>
			<if test="createUser!=null">
			createUser = #{createUser},
			</if>
			<if test="modifyTime!=null">
			modifyTime = #{modifyTime},
			</if>
			<if test="modifyUser!=null">
			modifyUser = #{modifyUser},
			</if>
		</set>
		WHERE
			userId = #{userId} AND roleId = #{roleId}
	</update>

	<!-- 根据主键删除 -->
	<delete id="deleteByPrimaryKeys">
		DELETE FROM
			admin
		WHERE
			userId = #{userId} AND roleId = #{roleId}
		</foreach>
	</delete>
	
	<!-- 根据主键查询 -->
	<select id="getByPrimaryKey" resultMap="resultMap" parameterType="long">
		SELECT
		<include refid="columns"/>
		FROM
			admin
		<where>
		    <if test="userId!=null">
		       userId = #{userId}
		    </if>
		    <if test="roleId!=null">
	        AND roleId = #{roleId}
		    </if>
		</where>
		<!-- 防止参数都为空，变成全表删除 -->
			AND 1=1
	</select>
	
	<!-- 根据参数查询列表 -->
	<select id="selectByCriteria" resultMap="resultMap" parameterType="Admin">
		SELECT
		<include refid="columns"/>
		FROM
			admin
		<include refid="defaultCriteria"/>
		<include refid="orderByClause"/>
	</select>
	
	
	<!-- 默认的查询条件 -->
	<sql id="defaultCriteria">
		<where>
			<!-- 关键字查询字段 -->
			<if test="keyword!=null and keyword!=''">
				AND name like "%"#{keyword}"%"
			</if>
		</where>
	</sql>
	
	<!-- 排序字段 -->
	<sql id="orderByClause">
		<if test="orderByClause!=null">
			${orderByClause}
		</if>
	</sql>
	
	<!-- 默认的数据库字段 -->
	<sql id="columns">
		userId,roleId,createTime,createUser,modifyTime,modifyUser
	</sql>

	<!-- 默认的数据库映射 -->
	<resultMap type="AllotRole" id="resultMap">
		<id property="userId" column="userId" />
		<id property="roleId" column="roleId" />
		<result property="createTime" column="createTime" />
		<result property="createUser" column="createUser" />
		<result property="modifyTime" column="modifyTime" />
		<result property="modifyUser" column="modifyUser" />
	</resultMap>
</mapper>